version: 1
applications:
  - appRoot: apps/web
    frontend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm
            - cd ../.. && pnpm install
        build:
          commands:
            - cd ../.. && env | grep -e NEXT_PUBLIC_ >> apps/web/.env.production
            - cd ../.. && pnpm run build --filter=@peakIntel/web
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - .next/cache/**/*
          - ../../node_modules/**/*
